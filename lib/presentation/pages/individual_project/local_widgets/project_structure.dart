import 'package:flutter/material.dart';

class ProjectStructure extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Project structure'),
    );
  }
}
